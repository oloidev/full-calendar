export type TProvider = {
    id: string;
    name: string;
    avatarUrl?: string;
};

export type TLocation = {
    id: string;
    name: string;
    color: string;
};

export type TPatient = {
    id: string;
    name: string;
};
