export type TProvider = {
    id: string;
    name: string;
    avatarUrl?: string;
    color: string;
};

export type TLocation = {
    id: string;
    name: string;
};

export type TPatient = {
    id: string;
    name: string;
};
